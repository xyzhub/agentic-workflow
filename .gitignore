.DS_Store
evals/results/

.env

# codex adapter run artefacts: keep the distillate JSON (n=1 evidence), drop the raw streams
.plans/runs/*.events.jsonl
.plans/runs/*.raw.txt
.plans/runs/*.stdout.jsonl
.plans/runs/*.stderr.txt
