---
status: living
owner-agent: planner
refresh-trigger: event
---

# Obligations register

_The repo-level parking place for deferred obligations that outlive a single
mission (WORKFLOW.md §5) — a promised action with an observable condition and
no trigger yet, probed at beats (`/agentic-workflow:settle`, the
`obligations-due` hook, `check.md`) instead of being lost the moment a
mission's own `## Closing` block closes. Deploys to `.plans/OBLIGATIONS.md`. A
`[~]` row in any mission ledger's `## Closing` block promotes here with a
verbatim copy and a `→ OB-<n>` back-reference — this file is where that
promise survives its mission._

_Grammar, one line each:_ `- [ ] OB-<n> · added YYYY-MM-DD (<source>) — do:
<action> — when: <observable condition> — probe: <command | manual>` _where
`<n>` is the next unused integer, `<source>` names who added the row (a
mission-ledger path or an agent name), and `<observable condition>` is a state
a probe can check — **never a clock** ("weekly" is not a condition; "a new
compaction record exists in the corpus" is). `probe:` is either a runnable
command (backticked) or the literal word `manual` when no command can decide
it. Rows are **never deleted**: a fired row keeps its line and appends
`· fired YYYY-MM-DD (<evidence>)`._

- [ ] OB-1 · added 2026-08-11 (.plans/compaction-continuity.state.md:75) — do: re-measure the handoff-budget n=1 compaction bands (the ≈6.73 bytes/token anchor and the trigger threshold are single-observation, config-dependent figures) — when: upstream compaction behavior changes OR the local transcript corpus gains ≥3 new true-compaction records — probe: manual
- [ ] OB-2 · added 2026-08-11 (.plans/compaction-continuity.md:142) — do: run the D4b cross-mission token re-measurement so quoted figures outgrow n=1 per corpus — when: ≥2 further missions' transcripts exist post-v1.43 — probe: manual
- [x] OB-3 · added 2026-08-11 (.plans/deferred-obligations.state.md S4) — do: reap the three concluded integration branches (mission/compaction-continuity-integration, mission/context-economy-integration, mission/sales-doc-architecture-integration), local and remote — their merge PRs are closed and CI was green on each merge commit, but the protected set in commands/settle.md shields all mission/*-integration branches unconditionally — when: the human rules the concluded-integration class reapable (or the protected-set clause is refined to shield only unconcluded missions) — probe: manual
  · condition MET 2026-08-11: human ruled **reapable once concluded** — the shield applies only while a mission is open (no `Closed:` stamp / unmerged PR); S6 amends `commands/settle.md`'s protected-set clause accordingly, and the three branches reap at the next settle run
  · fired 2026-08-11 (settle close, deferred-obligations S8): all three reaped local+remote — compaction-continuity-integration (was 9753715, PR #32 merge a75b844… lint:success) · context-economy-integration (was 24646b5, PR #31 merge 2c8487f… lint:success) · sales-doc-architecture-integration (was c5a810d, PR #30 merge 7e56f2e… lint:success). Probe lesson logged: the ladder's full-SHA warning is real — a fabricated tail 422s; derive SHAs from `gh pr view --json mergeCommit`, never by hand.
  · clause amended 2026-08-11 (S6, `commands/settle.md` protected set): a `mission/*-integration` branch is shielded only while its mission is open — an unmerged integration PR, or a `## Closing` block lacking the `Closed:` stamp; concluded integrations (pre-grammar ledgers judged by PR + deploy-green alone) fall through to the step-2 ladder. The reap itself stays parked for the next `/agentic-workflow:settle` run

- [x] OB-4 · added 2026-08-11 (deferred-obligations `## Closing` OB-a, promoted at the S8 settle close) — do: delete the deferred-obligations mission's four phase branches + integration branch, local and remote — when: the integration PR is merged by the human AND CI concluded green on the merge commit — probe: `gh pr view <n> --json state,mergeCommit` + `gh api repos/<owner>/<repo>/commits/<full-sha>/check-runs`
  · fired 2026-08-11 (post-#33 settle): PR #33 MERGED, merge `513e40a` lint:success; reaped local+remote — p1 (was b36003e) · p2 (was 56bc882) · p3 (was f669a86) · p4 (was 9d00c3f) · integration (was f090bda) · plan/deferred-obligations (was 368bbfe). Remote heads now: main + 3 protected pre-workflow keeps only.
- [x] OB-5 · added 2026-08-11 (deferred-obligations `## Closing` OB-b, promoted at the S8 settle close) — do: live-verify the obligations-due hook fires in a real session — when: the shipped version is installed (plugin update + reload, post-merge) — probe: manual
  · fired 2026-08-16 (human ruling — owner confirmed the post-merge reinstall; installed release is `~/.claude/plugins/cache/xyz/agentic-workflow/1.43.0`, sourced from `github.com/xyzhub/agentic-workflow.git`). The hook fired unprompted at this session's SessionStart: `📌 Deferred obligations may be due — 7 register row(s) in .plans/OBLIGATIONS.md + 0 mission-ledger ## Closing row(s) still unticked`, oldest row quoted (OB-1) and correctly self-labelled `Grep-only advisory (no conditions were probed) — run /agentic-workflow:settle to probe each row`. Advisory-only contract held: it named the count and deferred probing to this settle run, which then proved rung 2 on both reaped branches. Count read 7 because the session opened on `feat/impeccable-v2` pre-merge; `main` carried 6.
- [x] OB-6 · added 2026-08-11 (deferred-obligations `## Closing` OB-c, promoted at the S8 settle close) — do: confirm the five legacy trios still pass lint on the installed release — when: the release lands on the default branch — probe: `node tools/lint.mjs` on a fresh checkout
  · fired 2026-08-11 (settle run, post-#35): fresh `--depth 1` clone of `main` @ `8d2e15d` (v1.43.0) — `lint: clean` · `hook-test: clean` · `context-attrib selftest: clean`. All five legacy trios pass on the released code.
- [x] OB-7 · added 2026-08-11 (ckpt-p4 finding 1 — ckpt-p3's folds F1/F2 were dropped with no disposition, plus two low lint leaks it found) — do: add the >140-char-row harness case (hook-test) · add a carrying-commit command to settle.md recipe #2 · close the digitless `→ OB-` promotion-ref lint leak · extend the clock-leak pattern to `after N <units>` — when: the next session that edits tools/hook-test.mjs, tools/lint.mjs, or commands/settle.md is briefed (fold these in) — probe: manual
  · fired 2026-08-16 (graph-engineering P1 session — the briefed session edited all three named files, so all four folds landed): (1) the >140-CODEPOINT harness case (`9ee0281`) — fixture padded with em-dashes so byte 140 falls mid-character, verified to discriminate (a byte-slice of the same row yields `——�`, which the assertion rejects); hook-test 73 → 74 ok. (2) the carrying-commit command in `settle.md` recipe #2 (`0a9a382`) — `git rev-list --ancestry-path --reverse <tip>..origin/<default> | head -1`, verified against both branches this session reaped (6fe8c4e → a93eb89 PR #37, 7c52ff8 → 78964a5 PR #36). (3) the digitless `→ OB-` promotion-ref leak (`cc38924`) — `/→ OB-/` → `/→ OB-\d+/`, so a `[~]` row can no longer promote to nothing and still clear the close gate. (4) the clock-leak `after N <units>` branch (`cc38924`) — unit alternation extracted to `TIME_UNITS` and shared with the cadence branch; anti-overreach re-verified after the change (`after 3 phases are merged`, `after 3 missions ship`, `every phase PR is merged` all still pass)
  · evidence corrected 2026-08-16 (pre-merge council, appended — the line above stands as written): fold (2)'s verification was of the WRONG CASE. The carrying-commit command was checked against 6fe8c4e→a93eb89 and 7c52ff8→78964a5, but both of those branches had their own merged PRs, i.e. the ordinary rung-1/2 path — the no-PR absorbed-as-ancestry sub-case the command exists for was never exercised. Reproduced in a synthetic repo: the shipped form returned an intermediate feature-branch commit that never landed on the default branch and has no CI run, so `gh run list --commit` would return `[]` and surface a green branch forever. Corrected with `--merges --topo-order` and re-verified across four cases (both real merges, the absorbed case, and tip-is-head). Fold (4) was likewise rebuilt after a review measured the clock guard 25/44 wrong; see `tools/lint-test.mjs`. All four folds now stand, on corrected implementations
- [ ] OB-8 · added 2026-08-11 (backend, feat/impeccable-integration) — do: live-verify the impeccable peer-plugin integration (design-facing agents load + cite its actual rules, the doctor environment row fires, and the absence path stays citation-free; v2 surface, appended feat/impeccable-v2: the detect gate exercised fail-open (frontend + reviewer run the detector CLI, severities in the return, a CLI failure reported-and-continued), the DESIGN.md round-trip (designer authors the brand system in impeccable's spec-compliant DESIGN.md with PRODUCT.md cross-referencing the PRD), and the stage-map vocabulary appearing in briefings) — when: impeccable is installed alongside a UI-surface venture session — probe: manual
- [x] OB-9 · added 2026-08-11 (release/v1.43.0 — three merged PRs deferred the version bump to a release session that had no trigger; OB-5/6/8's install conditions silently depended on it) — do: give the release a trigger — the `## Closing` template gains a row "version bumped + stamped, if the mission's CHANGELOG entry names a version", so settle's close gate refuses an unversioned ship — when: the next session editing templates/mission-state.md or commands/settle.md — probe: manual
  · fired 2026-08-16 (`5821fcf` — this session edited both named files). `templates/mission-state.md`'s `## Closing` block gains the seeded row `version bumped + stamped`, so the close gate that already refuses any unticked `[ ]` row now refuses an unversioned ship with no new machinery; a mission whose CHANGELOG entry names no version fires the row immediately with `no version named` as evidence rather than deleting it. `commands/settle.md` §5 documents the coupling and the incident behind it. The generalized form of this failure — one loop silently gating another — shipped the same day as the `depends-on: OB-<n>` grammar (`f535580`)
  · evidence corrected 2026-08-16 (pre-merge council, appended — the line above stands as written): two claims in it are wrong. (a) The `depends-on: OB-<n>` grammar it cites as shipping the same day was REVERTED before merge — five review lenses found it inert in every documented authoring shape (the tail was pushed off its `$` anchor by the mandatory `· fired …` append, and the indented-continuation form its own architecture memo recommended was invisible). (b) The seeded row's probe grepped `.claude-plugin/plugin.json`, which does not exist at repo root here and exists in no consumer venture — it errored with exit 2, and under the fail-closed probe rule that row could never fire by probe. Both corrected: the probe is now `manual` with the location deferred to §10, and settle.md §5 no longer claims an unversioned ship is impossible — the gate binds MISSION ledgers only, and the session-altitude half is now OB-12
- [ ] OB-10 · added 2026-08-16 (graph-engineering assessment, this session — three candidate memory stores surveyed against the §13 "never a database; a DB may exist only as the §10 memory accelerator" ruling) — do: trial a §10 memory/recall accelerator for the `user`+`feedback` slice only, leaving `.plans/`, the register and the commons git-native — lead candidate `agent-memory.dev` (Apache-2.0, local JSON, no external DB, native Claude Code plugin); rejected on survey: Honcho (AGPL-3.0 + Postgres/pgvector, models peers not project facts) and TencentDB-Agent-Memory (proxy-shaped door, four-way overlap with `.remember/` + skills + commons + codegraph) — measure NET context (recall gain MINUS its own 54-tool MCP surface and 12-hook output) against the frozen eval set, and extend `tools/hook-test.mjs` for collision with `beat-enforcer` on the five events already wired before anything is installed — when: the frozen-evals + paired-metrics work has landed AND curator's escalation trigger fires (a screenful of `commons/index.md` entries per type, OR a consumer observed consulting more than one entry) — probe: manual
  · SUPERSEDED-CONDITION NOTE 2026-08-16 (appended, not rewritten — the row's `when:` above is restored verbatim as authored). The original condition names the frozen-evals + paired-metrics work, which this session's own council falsified and which was then dropped, so the row is currently unfireable as written. A pre-merge audit found the earlier in-place rewrite of this `when:` doubly wrong: the register is append-only (the OB-3 precedent it cited APPENDS lines and leaves its `when:` byte-intact), and the replacement condition — `evals/results/` tracked in git — is itself unfireable today, since that path is gitignored with zero tracked files and nothing schedules reversing that. The substance stands: an accelerator must not be adopted on a vendor's token-reduction claim without a measuring stick. Re-condition this row when a measuring stick actually exists; do not rewrite the line above to make it look live.
- [ ] OB-11 · added 2026-08-16 (staging-gap assessment, this session — §10 records "merge to `main` IS the release", so there is no pre-release environment and every live behaviour is verified post-hoc; OB-5 and OB-8 both exist only to compensate for that absence) — do: stand up a pre-release staging channel (a second marketplace on a branch or local source, with the released copy uninstalled first so `beat-enforcer` cannot fire twice) so live-only behaviour is verifiable before merge instead of via post-hoc live-verify obligations — when: a second live-only defect reaches `main` (the v1.39.1 Stop-hook loop is the first) — probe: manual
- [ ] OB-12 · added 2026-08-16 (pre-merge council on PR #38 — OB-9's fix binds mission ledgers only, and every ship since v1.43.0 has been session-altitude, so the incident OB-9 records is only half-addressed) — do: give session-altitude ships a version trigger too — as observed on 2026-08-16 the manifest read 1.43.0 while two merged PRs (#36, #37) had already landed past it, so consumers running `/plugin update` received post-1.43.0 code labelled 1.43.0 (v1.44.0 in this PR corrects the number, not the missing trigger) — when: the next session that edits commands/end.md, commands/release.md, or the session-close fall-through — probe: manual
- [ ] OB-13 · added 2026-08-17 (re-review F1 on PR #38 — the CHANGELOG promises the reverted work "returns as its own change" and no row fires that promise; the revert also re-exposed a blind spot the reverted work had found) — do: rebuild the declared-edge grammar as an INDENTED continuation line (the form its own architecture memo recommended and the inline form could not survive, since the mandatory `· fired …` append pushes an end-anchored tail off `$`), with the positive-capture assertion that memo required — a passing row carrying a valid edge must assert the capture is non-empty, because asserting only that bad rows fail cannot distinguish working from inert; and close the `obBullets` blind spot it surfaced, where a continuation line that is neither `- ` nor indented is silently dropped and is therefore invisible to every check — when: the next session that edits tools/lint.mjs's obligation-row checks — probe: manual
