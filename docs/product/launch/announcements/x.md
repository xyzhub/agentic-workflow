# Announcement — X/Twitter (thread)

**Channel**: X/Twitter, posted from the owner's account.
**Voice**: plain, technical, first-person — **best guess, no flight plan
exists**; owner corrects before publishing. No hashtag soup, no "🚀".
**Format constraints**: ≤ 280 chars per post; thread of 7; first post must
stand alone; link once (last post), media early if available.

## Draft (ready to paste)

**1/**
Coding agents give you speed without governance. The ecosystem's answer to
"more autonomy" is raw loops — no gates between the loop and main.

I built the missing layer as a Claude Code plugin. 30 releases in, never
announced. Thread:

**2/**
Agentic Workflow is a venture lifecycle, not a dev loop: V0 idea → V6
operate, with an exit gate on every stage.

Superpowers/BMAD/Spec Kit cover plan→code. This covers idea validation,
brand, business model, build, launch, and operations. Different layer.

**3/**
The rule that makes it safe: agents propose, the human decides.

Guardrail hooks fail closed — no pushes to main, no merges, no deploys, no
spending, no publishing. ~16 role agents prepare everything; you fire.

**4/**
It runs unattended without going feral. The ledger is the state, so a
recurring `/loop /mission continue` executes one brief per tick in a fresh
context. Crash-safe by construction.

**5/**
And the gates come to you: a private Telegram/Slack owner channel with
tap-to-decide — nonce-bound, identity-pinned, fail-closed. Merge/deploy/
spend gates always carry a link instead: you fire those where they live.

**6/**
Honesty, since it's MIT: no moat, it's all forkable prose. The in-repo evals
test protocol compliance, not whether it generalizes to YOUR repo. That's
the open question — `/adopt` on an existing project answers it in minutes
and hands you a gap report.

**7/**
Install:

/plugin marketplace add xyzhub/agentic-workflow
/plugin install agentic-workflow@xyz

Then /adopt on an existing repo, /autopilot "<idea>" for a new one, /next
when lost.

Repo: github.com/xyzhub/agentic-workflow

## Notes for the publisher

- Post after the HN submission is live; if HN gets traction, quote-reply
  your own thread with the HN link rather than editing.
- Post 3 and 5 are the screenshot slots: the push-block hook firing, and a
  gate arriving on a phone with inline buttons. Real screenshots beat all
  copy in this thread.
- Character counts are near the limit on 1, 5, and 6 — recount after any
  edit.
