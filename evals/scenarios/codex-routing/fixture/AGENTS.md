# Conventions

- Read `docs/WORKFLOW.md` §10 first.
- Role prompts live in the plugin's `agents/`; obey the brief in `.plans/`.
- Return the bounded distillate. Never edit `.plans/`, never commit/push/merge.
