---
budget-usd: 3
pass-bar: 0.7
judge-files: src/components/LandingHero.vue, src/components/SignInForm.vue
---
Spawn the frontend agent to build the two components described in docs/product/brief.md for this Vue 3 + Vite project: src/components/LandingHero.vue (the marketing landing hero) and src/components/SignInForm.vue (the sign-in form). It must honor the design tokens in src/assets/tokens.css. Do not install dependencies, run a build, or start a dev server — author the component files only. Report what was built.
