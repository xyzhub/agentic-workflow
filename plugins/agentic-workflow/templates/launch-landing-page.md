---
status: semi-static
owner-agent: marketing
refresh-trigger: event
---

# {{PROJECT_NAME}} — Landing Page (V5)

Outline + copy for the `frontend` agent to build with the chosen design
tokens. Copy inherits its claims from `positioning.md` and its **facts from
`docs/product/catalog/features.md`** — only rows with `marketable: yes` and
`status: live` (§6.1); a "What's new" block draws rows whose `last change` is in
the release window. Never from the CHANGELOG. Any price shown references
`business/pricing.md` (the single price source) — never restate the tiers.
Promise nothing the current version doesn't do — and every feature named below
carries its `F-n` id in an HTML comment so the reviewer can trace it.

## Hero
_Headline (the sharpest messaging pillar), subline, primary action._

## Proof
_How we show it works — screenshot, demo, numbers, quote. Evidence, not
adjectives._

## Objections answered
_The 2–3 reasons the ICP hesitates, each answered honestly._

## Final call to action
_One action, same as the hero. No competing asks._
